# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json
pnpm-lock.yaml
yarn.lock

# OS cruft
.DS_Store
Thumbs.db

# Env & local configs
.env
.env.*
*.local

# Build artifacts
dist/
build/
.cache/
.tmp/

# Your image pipeline outputs (pick ONE policy)
# EITHER commit optimized/ (fast deploys, heavier repo)…
# optimized/
# …OR ignore it and build in CI (clean repo, requires pipeline):
optimized/